# 문제 1번

* 문제 레벨 : 0
* 문제 종류 : 요구사항 구현
* 문제 링크 : https://pyalgo.co.kr/?page=1
* 통과 여부 : Y

```python
def solution(data):
 answer=""
 for i in data:
  new_string=i.replace("+","1").replace("-","0").replace(" ","")
  decimal=int(new_string,2)
  answer+=chr(decimal)

 return answer

    
```
// 문자열을  바꿔주는 코드
먼저, 어떤 문자열을 바꾸기 위해서는 어떤 문자열을 어떤 문자열로 바꿀 것인지 정확하게 명시해야 합니다. 그리고 그것을 어떤 상황에서 바꿀 것인지에 따라서 코드가 달라질 수 있습니다. 그러나 가장 간단한 예시를 들어보자면, 파이썬에서 문자열을 바꾸는 코드는 다음과 같이 작성할 수 있습니다.

```
original_string = "원본 문자열입니다."
new_string = original_string.replace("원본", "바꿀")
print(new_string)
```

여기서는 "원본"을 "바꿀"로 바꾸었습니다. `replace` 함수는 문자열에서 특정 문자나 문자열을 찾아서 새로운 문자나 문자열로 바꿔주는 역할을 합니다. 이렇게 하면 출력 결과는 "바꿀 문자열입니다." 가 됩니다.

// 2진수를 10진수로 바꾸는 코드
다음은 Python 코드입니다.

```python
binary = "1101" # 바꾸려는 2진수
decimal = int(binary,2) # int() 함수를 이용하여 2진수를 10진수로 변환
print(decimal) # 결과 출력
```

출력:

```
13
```

위 코드에서 `int()` 함수는 첫 번째 매개변수로 전달된 문자열을 두 번째 매개변수로 전달된 진수로 변환해줍니다. 위 코드에서는 `binary` 변수에 저장된 문자열을 2진수로 간주하고, `int()` 함수의 두 번째 매개변수로 2를 전달하여, 2진수를 10진수로 변환해줍니다. 변환된 결과는 `decimal` 변수에 저장되며, `print()` 함수를 이용하여 출력합니다.
// 아스키 코드 변환 구하는 코드 
아스키 코드를 구하는 코드는 다음과 같습니다.

```python
# 문자열 입력
c = input("문자를 입력하세요: ")

# ord() 함수를 이용하여 아스키 코드로 변환
ascii_code = ord(c)

# 결과 출력
print(f"{c}의 아스키 코드는 {ascii_code}입니다.")
```

위의 코드에서 `ord()` 함수는 인자로 주어진 문자의 아스키 코드값을 반환합니다. 예를 들어, `ord("A")`는 65를 반환합니다.
여기에서는 `chr()` 함수를 써서 구해야함 

