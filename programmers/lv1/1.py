def solution(n):
    
    n=str(n)
    answer=list(n)
    
    result = [int(i) for i in answer]
    result=result[::-1]
    return result

# array = ['1','2','3']
# intarray = list(map(int,array))

# array=['1','2','3']
# qw=[int(i) for i in array]

